import 'package:flutter/material.dart';
import '../widgets/ai_chat_menu.dart';

class ChatPage extends StatelessWidget {
  const ChatPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const AiChatMenu();
  }
}
